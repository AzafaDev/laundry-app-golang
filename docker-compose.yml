services:
  postgres:
    image: postgres:16
    container_name: laundry-postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: laundry_go
    ports:
      - "5432:5432"
    volumes:
      - "laundry_pg_data:/var/lib/postgresql/data"

volumes:
  laundry_pg_data:
